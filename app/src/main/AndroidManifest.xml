<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.koshanapp">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Post" />
        <activity android:name=".OurGuiders" />
        <activity android:name=".DetailsGuider" />
        <activity android:name=".Reset_Password" />
        <activity android:name=".Guid_Registration" />
        <activity android:name=".User_registration" />
        <activity android:name=".Home_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ImageList"
            android:label="Image List"
            android:parentActivityName=".Home_activity" />
    </application>

</manifest>